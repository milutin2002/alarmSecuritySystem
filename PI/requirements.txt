# Web server
Flask==2.3.2
Werkzeug==2.3.7

# Computer Vision (stable with NumPy 1.26)
numpy==1.26.1
opencv-python==4.8.0.74

# Environment variables
python-dotenv==1.0.0

# Firebase (if you're using it)
firebase-admin==6.2.0

# Optional: HTTP requests (if calling APIs)
requests==2.31.0
